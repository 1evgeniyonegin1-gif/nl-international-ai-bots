# Интеграция YandexART (Генерация изображений)

## Описание

YandexART интегрирован в контент-менеджер бот для автоматической генерации изображений к постам. Использует тот же Yandex Cloud аккаунт, что и YandexGPT.

## Настройка

### 1. Переменные окружения

Добавьте в `.env`:

```env
# YandexART (генерация изображений) - использует те же ключи Yandex Cloud
YANDEX_ART_ENABLED=true  # Включить генерацию изображений
YANDEX_ART_WIDTH=1024    # Ширина изображения (кратно 8, 256-1024)
YANDEX_ART_HEIGHT=1024   # Высота изображения (кратно 8, 256-1024)
```

YandexART использует те же credentials что и YandexGPT:
- `YANDEX_SERVICE_ACCOUNT_ID`
- `YANDEX_KEY_ID`
- `YANDEX_PRIVATE_KEY`
- `YANDEX_FOLDER_ID`

### 2. Установка зависимостей

YandexART требует дополнительную библиотеку:

```bash
pip install httpx==0.26.0
```

Или установите все зависимости:

```bash
pip install -r requirements.txt
```

## Как работает

### Автоматическая генерация при создании поста

При генерации поста через `/generate`, если `YANDEX_ART_ENABLED=true`, бот:

1. Генерирует текст поста (YandexGPT/GigaChat/GPT-4)
2. Автоматически генерирует изображение (YandexART)
3. Показывает админу пост с изображением на модерацию

### Управление изображениями

Админ может:

- **🖼 Добавить картинку** — генерирует изображение к существующему посту
- **🖼 Новое изображение** — перегенерирует изображение (новый seed = другой результат)
- **🚫 Без картинки** — удаляет изображение из поста

### Публикация с изображением

При публикации поста (`✅ Опубликовать`), если у поста есть изображение:
- Изображение публикуется вместе с текстом
- Если не удалось отправить изображение — публикуется только текст

## Архитектура

### Файлы

- `shared/ai_clients/yandexart_client.py` — клиент YandexART API
- `content_manager_bot/ai/content_generator.py` — интеграция в генератор контента
- `content_manager_bot/handlers/callbacks.py` — обработчики кнопок изображений
- `content_manager_bot/handlers/admin.py` — автогенерация при создании поста
- `content_manager_bot/utils/image_helpers.py` — вспомогательные функции

### База данных

В таблице `content_posts` есть поля:

```python
image_url: Optional[str]     # Base64-encoded изображение
image_prompt: Optional[str]  # Промпт использованный для генерации
image_status: Optional[str]  # pending/generated/approved/rejected
```

## API YandexART

### Процесс генерации

1. **JWT токен** — создаётся из приватного ключа
2. **IAM токен** — получается через JWT (кешируется на 11 часов)
3. **Запрос генерации** — отправляется асинхронно, возвращает `operation_id`
4. **Polling** — каждые 2 секунды проверяется статус операции
5. **Результат** — base64-encoded JPEG изображение

### Промпты для разных типов постов

YandexART автоматически генерирует промпты на основе типа поста:

- **product** — "Красивая фотография продуктов здорового питания, яркие цвета..."
- **motivation** — "Вдохновляющее изображение, успешные люди, позитивная атмосфера..."
- **news** — "Современный офис, бизнес-встреча, корпоративный стиль..."
- **tips** — "Инфографика, яркие иконки, обучающий материал..."
- **success_story** — "Счастливые успешные люди, достижение целей..."
- **promo** — "Яркий рекламный баннер, специальное предложение..."

Промпт формируется автоматически с учётом:
- Типа поста
- Первых 100 символов текста поста (контекст)
- Общих параметров качества (4K, профессиональная фотография, без текста)

## Производительность

- **Время генерации**: 30-60 секунд на изображение
- **Timeout**: 120 секунд (настраивается в `max_wait_seconds`)
- **Размер**: ~100-300 KB (JPEG, 1024x1024)
- **Формат**: Base64-encoded JPEG

## Лимиты Yandex Cloud

По состоянию на 2026 год:

- **Бесплатный tier**: ~1000 запросов/месяц
- **Платный**: от 1₽ за запрос

Проверяйте актуальные лимиты в [Yandex Cloud Console](https://console.cloud.yandex.ru/)

## Отключение YandexART

Если хотите отключить автогенерацию изображений:

```env
YANDEX_ART_ENABLED=false
```

Или удалите/закомментируйте переменную. Бот продолжит работать без изображений.

## Troubleshooting

### Изображения не генерируются

1. Проверьте что `YANDEX_ART_ENABLED=true`
2. Проверьте credentials Yandex Cloud (те же что для YandexGPT)
3. Проверьте квоты в Yandex Cloud Console
4. Проверьте логи: `docker-compose logs content_manager_bot`

### Ошибка "IAM token expired"

Токен автоматически обновляется каждые 11 часов. Если ошибка:
- Проверьте что время на сервере правильное
- Проверьте что приватный ключ валидный

### Ошибка "Operation timed out"

Генерация заняла >120 секунд:
- Повторите попытку (нажмите "🖼 Новое изображение")
- Или отключите изображения для этого поста ("🚫 Без картинки")

### Изображение не показывается в Telegram

1. Проверьте что изображение валидное (base64 декодируется)
2. Проверьте размер (<10 MB для Telegram)
3. Если проблема повторяется — откройте issue в репозитории

## Примеры

### Генерация поста с изображением

```python
# В коде уже реализовано автоматически
content_generator = ContentGenerator()

# Проверяем доступность
if content_generator.is_image_generation_available():
    # Генерируем изображение
    image_base64, prompt = await content_generator.generate_image(
        post_type="product",
        post_content="Сегодня расскажем о новом продукте..."
    )
```

### Ручная генерация через YandexART клиент

```python
from shared.ai_clients.yandexart_client import YandexARTClient

client = YandexARTClient()

# Простая генерация
image_base64 = await client.generate_image(
    prompt="Красивый закат над морем, 4K",
    width=1024,
    height=1024
)

# С seed для воспроизводимости
image_base64 = await client.generate_image(
    prompt="Красивый закат над морем",
    seed=42  # Один seed = одинаковый результат
)
```

## Roadmap

Планируемые улучшения:

- [ ] Возможность редактировать промпт вручную через UI
- [ ] Поддержка разных aspect ratio (не только 1:1)
- [ ] Кеширование изображений для одинаковых промптов
- [ ] Возможность загрузки своего изображения вместо генерации
- [ ] История генерации изображений с возможностью вернуться к предыдущим

## Документация Yandex Cloud

- [YandexART API](https://cloud.yandex.ru/docs/yandexgpt/concepts/yandexart)
- [IAM авторизация](https://cloud.yandex.ru/docs/iam/operations/iam-token/create-for-sa)
- [Квоты и лимиты](https://cloud.yandex.ru/docs/yandexgpt/concepts/limits)
