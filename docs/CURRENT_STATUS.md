# Текущий статус проекта — 17.01.2026

## ✅ Что работает

### Боты на сервере (VPS 194.87.86.103)
- AI-Куратор (@nl_curator_bot) — работает 24/7
- AI-Контент-Менеджер (@nl_content_bot) — работает 24/7
- Systemd сервис: `nl-bots.service`

### Контент-менеджер — рабочие функции:
- `/generate` — генерация постов (6 типов)
- `/pending` — посты на модерации
- `/stats` — статистика
- ✅ Опубликовать — публикация в Topics группы
- 📅 Запланировать — отложенная публикация
- 📝 Редактировать — редактирование через AI
- 🔄 Перегенерировать — перегенерация с фидбеком
- ❌ Отклонить — удаление поста

### Публикация в Topics группы
- GROUP_ID: -1003676349853
- Темы настроены (Products=7, Business=6, Training=5, News=4, Success=3, FAQ=2)
- Каждый пост содержит ссылку на AI-Куратора

---

## 🔧 Что нужно доделать

### 1. Автопостинг по расписанию (в процессе)
Код написан, но кнопки в `/schedule` не работают.

**Файлы:**
- `content_manager_bot/scheduler/content_scheduler.py` — планировщик
- `content_manager_bot/handlers/callbacks.py` — обработчики кнопок autosched:*

**Что должно работать:**
- `/schedule` → показывает меню
- Кнопка "Продукты" → включает автогенерацию каждые 8 часов
- Бот сам генерит пост и присылает с кнопками модерации

**Нужно отладить:** проверить логи почему кнопки не срабатывают

### 2. Генерация картинок к постам
Ещё не начато. Нужно:
- Подключить генератор изображений (DALL-E, Midjourney API, или бесплатный)
- Добавить отправку фото вместе с текстом

### 3. Переписать промпты
Ещё не начато. Нужно:
- Проанализировать рынок (как делают конкуренты)
- Создать стайлгайд для постов
- Переписать промпты в `content_manager_bot/ai/prompts.py`

### 4. Сменить нейросеть (опционально)
Сейчас используется GigaChat (бесплатно).
Можно подключить Claude/GPT для лучшего качества.

---

## 📁 Важные файлы

| Файл | Описание |
|------|----------|
| `shared/config/settings.py` | Все настройки (токены, ID) |
| `content_manager_bot/ai/prompts.py` | Промпты для генерации |
| `content_manager_bot/handlers/callbacks.py` | Обработчики кнопок |
| `content_manager_bot/scheduler/content_scheduler.py` | Автопостинг |
| `.env` | Переменные окружения |

---

## 🖥️ Команды для сервера

```bash
# Подключение
ssh root@194.87.86.103

# Обновить код
cd /root/nl-international-ai-bots && git pull

# Перезапустить ботов
systemctl restart nl-bots

# Проверить статус
systemctl status nl-bots

# Логи в реальном времени
journalctl -u nl-bots -f

# Последние 50 строк логов
journalctl -u nl-bots -n 50

# Редактировать .env
nano /root/nl-international-ai-bots/.env
```

---

## 🔑 Переменные .env на сервере

```
CURATOR_BOT_TOKEN=8546022649:AAH...
CONTENT_MANAGER_BOT_TOKEN=8522139282:AAH...
CHANNEL_USERNAME=@nl_Eleva
ADMIN_TELEGRAM_IDS=756877849
GIGACHAT_AUTH_TOKEN=...
DATABASE_URL=postgresql+asyncpg://nlbot:nlbot123@localhost:5432/nl_international
GROUP_ID=-1003676349853
CURATOR_BOT_USERNAME=@nl_curator_bot
TOPIC_PRODUCTS=7
TOPIC_BUSINESS=6
TOPIC_TRAINING=5
TOPIC_NEWS=4
TOPIC_SUCCESS=3
TOPIC_FAQ=2
CONTENT_MANAGER_AI_MODEL=GigaChat
```

---

## 📋 Чеклист для следующей сессии

1. [ ] Проверить почему кнопки автопостинга не работают (логи)
2. [ ] Доделать автопостинг
3. [ ] Добавить генерацию картинок
4. [ ] Переписать промпты для качества
5. [ ] (Опционально) Подключить Claude/GPT вместо GigaChat
