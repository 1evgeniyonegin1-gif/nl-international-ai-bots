# Reference Images - Быстрый старт

## Что это?

Система использует **реальные фотографии продуктов** вместо AI-генерации с нуля. AI берёт официальное фото продукта и улучшает только фон, оставляя продукт без изменений.

## Зачем?

**Проблема:** AI может неточно нарисовать упаковку, цвета, детали продукта.

**Решение:** Используем реальное фото + AI улучшает фон → получаем точное изображение продукта с профессиональным фоном.

## Как работает?

1. Бот генерирует текст поста о продукте
2. Автоматически находит упоминание продукта (например, "GreenFlash Vision Plus")
3. Ищет фото этого продукта в библиотеке `content/product_images/`
4. Если фото есть → использует **image-to-image** (реальное фото + улучшенный фон)
5. Если фото нет → использует **text-to-image** (полная AI-генерация)

## Быстрая настройка (3 шага)

### Шаг 1: Добавьте фотографии продуктов

Поместите официальные фото в папки:

```
content/product_images/
├── greenflash/       # Витамины
│   ├── vision_plus.jpg
│   ├── omega3.jpg
│   └── ...
├── lovely/           # Косметика
│   ├── face_cream.jpg
│   └── ...
├── energy_diet/      # Energy Diet
│   └── ...
└── other/            # Остальное
    └── ...
```

**Требования к фото:**
- Формат: JPG или PNG
- Размер: минимум 800x800, лучше 1024x1024
- Качество: высокое
- Фон: любой (AI изменит)

### Шаг 2: Обновите маппинг (уже есть шаблон!)

Файл `content/product_images/products_mapping.json` уже создан с примерами.

Добавьте свои продукты по аналогии:

```json
{
  "greenflash": {
    "new_product": {
      "name": "GreenFlash Новый Продукт",
      "image": "greenflash/new_product.jpg",
      "description": "Краткое описание"
    }
  }
}
```

### Шаг 3: Установите Pillow (для водяных знаков/логотипов)

```bash
pip install Pillow
```

Или:

```bash
pip install -r requirements.txt
```

## Использование

### Автоматически (рекомендуется)

Просто генерируйте посты как обычно:

```
/generate product
```

Бот автоматически:
- Определит продукт из текста
- Найдёт его фото
- Использует image-to-image режим

### Добавление водяного знака/логотипа (опционально)

```python
from content_manager_bot.utils.image_helpers import add_watermark, add_logo_overlay

# Водяной знак (текст)
image = add_watermark(image_base64, "NL International", "bottom_right")

# Логотип (PNG файл)
image = add_logo_overlay(image_base64, "content/resources/nl_logo.png", "bottom_right")
```

## Что уже сделано?

✅ **Структура папок** - `content/product_images/` создана
✅ **Маппинг продуктов** - `products_mapping.json` с примерами
✅ **Image-to-image в YandexART** - поддержка референсных изображений
✅ **Автоопределение продуктов** - по ключевым словам
✅ **Композитинг** - функции для watermark/logo
✅ **Интеграция в content_generator** - работает автоматически
✅ **Документация** - подробное руководство в `docs/REFERENCE_IMAGES.md`

## Что нужно сделать вам?

1. **Добавить фотографии продуктов** - официальные фото всех продуктов NL
2. **Обновить маппинг** - для каждого продукта в `products_mapping.json`
3. **(Опционально) Добавить логотип** - PNG файл в `content/resources/nl_logo.png`

## Примеры

### До (text-to-image)
AI рисует продукт с нуля → может неточно нарисовать упаковку, цвета, детали.

### После (image-to-image с референсом)
AI берёт реальное фото → улучшает фон → продукт выглядит точно как в реальности.

## Проверка работы

1. Добавьте фото продукта
2. Обновите `products_mapping.json`
3. Создайте пост: `/generate product`
4. Упомяните продукт в тексте (или пост будет сгенерирован автоматически)
5. Проверьте логи: должно быть `Using reference image for product: ...`

## Troubleshooting

**Продукт не находится автоматически?**
- Проверьте название в тексте поста
- Добавьте ключевые слова в `content_manager_bot/utils/product_reference.py`

**Фото не найдено?**
- Проверьте путь в `products_mapping.json`
- Убедитесь что файл существует

**Pillow не работает?**
- `pip install --upgrade Pillow`
- Проверьте версию: `pip show Pillow`

## Документация

- 📖 **Подробное руководство:** [docs/REFERENCE_IMAGES.md](docs/REFERENCE_IMAGES.md)
- 📁 **Структура ресурсов:** [content/resources/README.md](content/resources/README.md)
- 🛠 **Код:** [content_manager_bot/utils/product_reference.py](content_manager_bot/utils/product_reference.py)

## Следующие шаги

1. Соберите официальные фото всех продуктов NL
2. Добавьте их в `content/product_images/`
3. Обновите маппинг
4. Протестируйте генерацию постов
5. (Опционально) Настройте автоматическое добавление водяных знаков при публикации

---

**Готово!** Теперь посты о продуктах будут использовать реальные фотографии с улучшенным AI фоном. 🎉
